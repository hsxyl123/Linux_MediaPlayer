# Linux Media Player (Cine) - Ubuntu 完整部署指南
# ============================================================

# 第一步：安装系统依赖（apt）
# -----------------------------------------------------------
sudo apt update
sudo apt install -y python3 python3-venv python3-pip \
    python3-cairo python3-gi python3-gi-cairo \
    gir1.2-gtk-3.0 \
    mpv libmpv2 mpv-common \
    ffmpeg \
    pkg-config build-essential

# 第二步：创建虚拟环境
# -----------------------------------------------------------
python3 -m venv --system-site-packages linuxplayer
source linuxplayer/bin/activate

# 第三步：安装 Python 依赖（pip）
# -----------------------------------------------------------
pip install -r requirements.txt

# ============================================================
# pip 依赖列表（以下包通过 pip 安装）
# ============================================================
python-mpv>=1.0.0
requests>=2.31.0
