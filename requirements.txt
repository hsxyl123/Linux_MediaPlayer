# Simple Video Player - Requirements

# Core GUI Framework
PyQt5>=5.15

# Optional Backends (install via system package manager for better compatibility)
# VLC Backend (Recommended): sudo apt install vlc python3-vlc
python3-vlc; platform_system == "Linux"

# For development and testing
pytest>=7.0
pytest-qt>=4.0
